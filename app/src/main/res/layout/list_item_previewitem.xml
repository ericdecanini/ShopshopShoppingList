<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <import type="android.view.View" />

        <variable
            name="viewstate"
            type="com.ericdecanini.shopshopshoppinglist.entities.ShopItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:bind="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/item"
            style="@style/ShoppingListPreviewText"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:drawablePadding="8dp"
            android:gravity="center_vertical"
            android:text="@{viewstate.name}"
            android:visibility="@{viewstate != null ? View.VISIBLE : View.GONE}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            bind:drawableStart="@{viewstate.checked ? @drawable/checkbox_unchecked : @drawable/checkbox_checked}"
            tools:text="Oringe" />

        <TextView
            style="@style/ShoppingListPreviewEllipsisText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="8dp"
            android:paddingEnd="4dp"
            android:text="@string/ellipsis"
            android:visibility="@{viewstate == null ? View.VISIBLE : View.GONE}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
